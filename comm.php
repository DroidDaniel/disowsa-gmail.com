<?php date_default_timezone_set("America/New_York");require 'PHPMailer/PHPMailerAutoload.php';$cusname = $_GET['cusname'];$sub = $_GET['sub'];$comment = $_GET['comment'];$mailiden = $_GET['mailiden'];$monthNum  = date("m");$dateObj   = DateTime::createFromFormat('!m', $monthNum);$monthName = $dateObj->format('F'); $date = $monthName.date("-d-Y").' '.date("h:i:s A");$mail = new PHPMailer();$body = "<h1>Here are your customer details</h1>                        <p><strong> Name : </strong> $cusname</p>                  <p><strong> Mail : </strong> $mailiden</p>                   <p><strong> Subject : </strong> $sub</p>                  <p><strong> Comment : </strong> $comment</p>                 "                                    ; $mail->SMTPDebug  = 0;                     // enables SMTP debug information (for testing)      $mail->SMTPAuth   = true;                  // enable SMTP authentication      $mail->SMTPSecure = "ssl";                 // sets the prefix to the servier      $mail->Host       = "smtp.gmail.com";      // sets GMAIL as the SMTP server      $mail->Port       = 465;   // set the SMTP port for the GMAIL server      $mail->SMTPKeepAlive = true;      $mail->Mailer = "smtp";      $mail->Username   = "osthydesouza@gmail.com";  // GMAIL username      $mail->Password   = "vedamuthu";            // GMAIL password                   $mail->AddAddress('osthydesouza@gmail.com');                                       $mail->SetFrom('osthydesouza@gmail.com');      $mail->Subject = 'Contact Form - Testing';      $mail->AltBody = 'To view the message, please use an HTML compatible email viewer!'; // optional - MsgHTML will create an alternate automatically      $mail->MsgHTML($body);      $mail->Send();echo "OKAY";?>